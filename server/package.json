{
  "name": "aws-t12-task-scheduler-server",
  "version": "1.0.0",
  "description": "Server for scheduling AWS Lambda Functions as tasks",
  "main": "index.ts",
  "scripts": {
    "build": "npm run tsc",
    "clean-build": "rm -rf dist && npm run build",
    "dev": "npm run clean-build && node dist/index.js",
    "lint": "eslint ./**/*.{ts,tsx} --fix",
    "postinstall": "npm run tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc"
  },
  "author": "Arijit Saha <arijitbiley@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/express-jwt": "^6.0.1",
    "@types/node": "^14.14.33",
    "morgan": "^1.10.0",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "jwks-rsa": "^2.0.0",
    "mongoose": "^5.12.0"
  }
}
